#include <stdio.h>
/**
 * main - Aprogram that prints a line using the function prinf
 * Return: 0 (Succes)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		Return(0);
}
